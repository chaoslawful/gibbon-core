# 复制为同目录 .env 后填写。不要把 .env 提交到 git。
# GIBBON_API_BASE 不含末尾斜杠；推荐用 api.php（不依赖 Apache rewrite）。
GIBBON_API_BASE=http://localhost/api.php
GIBBON_API_TOKEN=gib_pat_replace_me
# 可选：skill 更新 manifest 地址（默认用 SKILL.md 内置地址；换服务器/本地测试时覆盖）
# GIBBON_SKILL_MANIFEST_URL=https://SKILL_HOST/skills/manifest.json
